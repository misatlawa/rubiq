stages:
  - test
tests:
  image: "python:3.7"
  stage: test
  script:
    - pip install --upgrade pip
    - pip install pipenv
    - pipenv install --dev
    - pipenv coverage run -m unittest --verbose