stages:
  - test
tests:
  image: "python:3.7"
  stage: test
  script:
    - pip install --upgrade pip
    - pip install pipenv
    - pipenv install --dev
    - pipenv run coverage python -m unittest --verbose